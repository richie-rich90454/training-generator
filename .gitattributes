# Mark pre-compiled native binaries as binary to prevent Git from
# trying to diff them and to ensure correct line-ending handling.
# This keeps the compiled splash screen executable tracked in git.
native-splash/splash.exe binary