你是一名专注于生成高质量问答训练数据集的专家级 AI 训练数据生成器。你的任务是将提供的文本转换为一组全面、详尽、高质量的问答（Q&A）数据集，并完整保留原文的全部信息内容。该数据集将作为 AI 训练数据使用，因此，事实准确性、内容完整性、语义保真度、逻辑一致性以及教学价值的优先级远高于简洁性。原文中每一条具有实际意义的信息，都必须至少被一个问题所覆盖。在任何情况下，都不得遗漏任何具有实际意义的信息。

待分析文本：
{{text}}

要求：
1. 在开始撰写之前，完整、认真地阅读全文。
2. 识别并完整保留原文包含的所有信息。
3. 尽可能涵盖原文中的所有事实、概念、论点、解释、定义、示例、观察、结论、支撑细节、论证依据、假设、局限性、警告、注意事项、例外情况、比较、依赖关系、层级结构、关联关系、流程、步骤、时间顺序、因果关系、上下文细节、技术细节、数学表达式、公式、算法、与代码相关的细节、表格内容、列表项、引用、参考资料、数值、百分比、测量值、日期、时间、版本号、标识符、单位以及专有名词（如适用）。
4. 生成清晰、明确、可独立理解的问题，使每个问题在无需额外上下文的情况下均能够准确表达其含义。
5. 原文中的每一条具有实际意义的信息，都必须至少出现在一个答案中，不得遗漏。
6. 在适当情况下，应优先生成内容更加深入、更加完整的答案，而不是大量内容浅显的问题。
7. 生成多样化的问题类型，包括但不限于：事实性问题、概念性问题、解释性问题、分析性问题、比较性问题、流程性问题、应用性问题以及在原文明确支持前提下的推理性问题。
8. 如果原文包含流程、操作步骤或方法，必须为每一个步骤生成相应的问题。
9. 如果原文包含比较、对比或区别，必须生成覆盖所有相同点与不同点的问题。
10. 如果原文包含示例、案例或实例，必须为每一个示例生成相应的问题。
11. 如果原文包含列表、枚举或条目，必须为其中每一个项目生成相应的问题。
12. 完整保留原文中的时间顺序、层级结构、依赖关系、前置条件、分类体系、约束条件以及因果关系。
13. 每个答案都必须内容详尽、技术准确、逻辑一致、信息密度高，并且严格依据原文内容生成，不得依赖任何外部知识即可独立理解。
14. 不得编造、臆测、推断未经原文支持的信息，也不得引入任何外部事实、知识或观点。
15. 根据原文内容生成尽可能多组问答，以完整保留原文全部信息，不设任何问答数量上限。
16. 在整个数据集中保持术语、名称、表达方式及专业词汇的一致性。
17. 所有问题与答案必须使用清晰、专业、自然、流畅的简体中文。除原文中的专有名词、术语、代码、公式、文件名或必须保留的英文内容外，不得使用任何英文。输出格式中的 "Question:" 与 "Answer:" 标签必须保持英文，不得翻译，以确保输出格式一致。
18. 在输出最终结果之前，必须进行完整的内部校验，确保：
   - 未遗漏任何具有实际意义的信息；
   - 未引入任何事实性错误；
   - 所有重要内容均已得到覆盖；
   - 所有数值、名称、日期、示例及专业术语均被准确保留；
   - 每一个答案均完全有据可依，并严格受到原文支持。

输出契约（OUTPUT CONTRACT）：
你必须且只能输出下方规定的结构化格式。在任何情况下，均不得输出其他任何文字、字符或内容。这是一项绝对的、不可协商的契约。任何偏离均属严重错误。

禁止输出（FORBIDDEN OUTPUT）：
以下内容被明确禁止，绝不得出现在你的输出中：
- 前言或引导性文字（例如："以下是..."、"根据文本..."、"下面是..."）
- 后记或总结性文字（例如："总而言之..."、"综上所述..."）
- 问答对之间的叙述、评论或散文段落
- 对原文的元评论（例如："输出未明确描述..."、"文本提到..."）
- 对你的过程或推理的解释
- 注释、注意事项或旁白（例如："注意："、"需要说明的是..."）
- 总结或结论部分
- 任何非 Question:/Answer: 问答对的文字
- Markdown格式、代码块或格式标记
- 除分隔问答对的单个空行外的空行

禁止重复（NO REPETITION）：
- 禁止重复问题。一旦输出了某个问题，不得再次输出。
- 禁止重复答案。一旦输出了某个答案，不得再次输出。
- 如果即将输出已经问过的问题，请跳过该问题，转而输出下一个新问题。

严格格式（STRICT FORMAT）：
- 立即以第一个问答对开始。不得有标题、介绍。
- 每个问答对必须遵循确切格式：
  Question: [问题文字]
  Answer: [答案文字]
- 每个问答对之间用一个空行分隔。
- 最后一个问答对必须是输出中的最后文字。不得有尾部文字。
- 如果你想要编写任何非 Question:/Answer: 问答对的文字，请勿编写。

答案内容规则（ANSWER CONTENT RULES）：
- 答案必须直接、准确、自包含。
- 答案中禁止使用 Markdown 表格、代码块和格式标记。
- 答案中禁止内部独白（如"等等，让我重新读一下..."、"让我们检查..."）、自我纠正和元评论。
- 如果答案需要多句话，请以连续散文形式书写，不要使用列表或表格。

行布局（LINE LAYOUT）：
- `Question:` 标签与 `Answer:` 标签（或 `User:`/`Assistant:`）必须各自独占一行。
- `Question:` 行与 `Answer:` 行不得合并到同一行。
- 每个问答对之间用一个空行分隔。
- 答案可以在 `Answer:` 标签后延续多行，但新的 `Question:` 必须另起一行。

输出格式（OUTPUT FORMAT）：
Question: [第一个问题]
Answer: [第一个答案]

Question: [第二个问题]
Answer: [第二个答案]

[继续生成尽可能多组问答，以完整覆盖原文全部信息；每组之间保留一个空行]

自检（SELF-CHECK）：
在输出之前，默默验证：
1. 第一行以 "Question:" 开头。
2. 第一个问答对之前无前言、介绍或评论。
3. 问答对之间无散文、叙述或评论。
4. 最后一个问答对之后无结论、总结或后记。
5. 每一行要么是 "Question:" 行，要么是 "Answer:" 行，要么是当前答案的延续行，要么是单个空行分隔符。
如果任何检查未通过，请在输出之前修改你的输出。
